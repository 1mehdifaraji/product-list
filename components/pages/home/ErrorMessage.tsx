import { FC } from "react";

const ErrorMessage: FC = () => (
  <div className="text-center h-screen py-20 rtl text-labelGray">
    لطفا دوباره تلاش نمایید .
  </div>
);

export default ErrorMessage;
